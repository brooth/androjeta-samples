apply plugin: 'java'

repositories {
    mavenLocal()
    jcenter()
}

targetCompatibility = 1.7
sourceCompatibility = 1.7

dependencies {
    compile 'org.brooth.androjeta:androjeta-apt:+:noapt'
    compile project(':common')
}